# AWS GPU 实例价格查询使用指南 📖

## 🎉 已完成功能

✅ **真实价格数据**：来自 instances.vantage.sh (AWS Pricing API)
✅ **多区域支持**：16 个 AWS 区域的实时价格
✅ **区域切换**：下拉菜单快速切换区域
✅ **实时搜索**：按实例名称、GPU 型号筛选
✅ **可用性检测**：自动显示实例在哪些区域可用

## 🚀 快速开始

### 方法 1：直接打开 HTML 文件
```bash
# 用浏览器打开
firefox /home/ubuntu/codes/aws-gpu/instances/p5.html

# 或者用 Chrome
google-chrome /home/ubuntu/codes/aws-gpu/instances/p5.html
```

### 方法 2：启动本地服务器
```bash
cd /home/ubuntu/codes/aws-gpu
python3 -m http.server 8000

# 然后在浏览器访问
# http://localhost:8000/instances/p5.html
```

## 💡 功能说明

### 1. 查看实例列表
打开 p5.html 后，你会看到：
- **p5en.48xlarge** (NVIDIA H200) - 12 个区域
- **p5.48xlarge** (NVIDIA H100) - 14 个区域
- **p5.4xlarge** (NVIDIA H100) - 10 个区域

### 2. 切换区域查看不同价格
页面顶部有区域选择器，包括：

**主要区域**：
- US East (N. Virginia) - us-east-1
- US East (Ohio) - us-east-2
- US West (Oregon) - us-west-2
- Europe (Ireland) - eu-west-1
- Asia Pacific (Tokyo) - ap-northeast-1
- ... 等

选择不同区域，价格会自动更新！

### 3. 价格对比示例

#### p5.48xlarge (NVIDIA H100) 价格对比

| 区域 | 按需价格 | 相对 us-east-1 的差异 |
|------|---------|---------------------|
| 🇺🇸 us-east-1 | $55.04/hr | 基准价格 |
| 🇺🇸 us-east-2 | $55.04/hr | 相同 |
| 🇺🇸 us-west-2 | $55.04/hr | 相同 |
| 🇯🇵 ap-northeast-1 | $68.80/hr | +25% ⚠️ |
| 🇮🇳 ap-south-1 | $66.05/hr | +20% ⚠️ |
| 🇧🇷 sa-east-1 | $92.47/hr | +68% ⚠️⚠️ |

**结论**：在美国区域最便宜！

#### p5en.48xlarge (NVIDIA H200) 价格对比

| 区域 | 按需价格 | 相对 us-east-1 的差异 |
|------|---------|---------------------|
| 🇺🇸 us-east-1 | $63.30/hr | 基准价格 |
| 🇺🇸 us-east-2 | $63.30/hr | 相同 |
| 🇺🇸 us-west-1 | $79.12/hr | +25% ⚠️ |
| 🇯🇵 ap-northeast-1 | $79.12/hr | +25% ⚠️ |
| 🇰🇷 ap-northeast-2 | $87.35/hr | +38% ⚠️⚠️ |

### 4. 搜索功能
使用搜索框快速查找：
- 输入 "H200" → 只显示 H200 实例
- 输入 "p5en" → 只显示 p5en 系列
- 输入 "8" → 显示所有 8 GPU 的实例

### 5. 排序功能
点击表头可以排序：
- **Instance Type** - 按实例名称
- **GPU Count** - 按 GPU 数量
- **vCPUs** - 按 CPU 核心数
- **Memory** - 按内存大小

再次点击可以切换升序/降序。

## 📊 数据说明

### 数据来源
- **来源**：instances.vantage.sh (AWS Pricing API)
- **更新时间**：2025-11-30
- **准确性**：AWS 官方定价数据

### 价格类型
目前显示的价格类型：
- ✅ **On-Demand**：按需定价（随时可用）
- ⚠️ **Spot**：部分实例有（待补充）
- ⚠️ **Reserved**：部分实例有（待补充）

### 区域说明
- ✅ **Available**：实例在该区域可用，显示真实价格
- ❌ **Not Available**：实例在该区域不可用，变灰显示

## 🔍 示例使用场景

### 场景 1：选择最便宜的区域
**需求**：训练大模型，需要 p5.48xlarge

**步骤**：
1. 打开 p5.html
2. 查看默认 us-east-1 价格：$55.04/hr
3. 切换到其他区域对比
4. **结论**：us-east-1, us-east-2, us-west-2 价格相同，选择最近的

### 场景 2：评估跨区域部署成本
**需求**：需要在美国和亚洲都部署

**步骤**：
1. 查看 us-east-1：$55.04/hr
2. 切换到 ap-northeast-1：$68.80/hr
3. **成本差异**：亚洲比美国贵 25%

### 场景 3：对比不同 GPU 型号
**需求**：H100 vs H200 性价比对比

**步骤**：
1. p5.48xlarge (H100): $55.04/hr (us-east-1)
2. p5en.48xlarge (H200): $63.30/hr (us-east-1)
3. **价格差**：H200 贵 15%
4. **性能差**：H200 显存 141GB vs H100 80GB (+76%)
5. **结论**：H200 性价比更高（显存多很多）

## 🛠️ 高级功能

### 更新价格数据
```bash
cd /home/ubuntu/codes/aws-gpu

# 下载最新数据
curl -s "https://instances.vantage.sh/instances.json" -o data/instances_full.json

# 重新提取和转换
python3 scripts/extract_gpu_instances.py
python3 scripts/convert_to_awsgpu_format.py
python3 scripts/merge_p5_data.py

# 刷新浏览器查看更新
```

### 扩展到其他系列
当前已有数据文件：
```bash
ls data/*_instances.js
```

可以参考 p5.html，创建其他系列的页面：
- g5.html
- g6.html
- p4.html
- 等等

## 📝 注意事项

1. **价格可能变化**：AWS 可能调整价格，建议定期更新
2. **Spot 价格波动**：Spot 价格实时变化，显示的是历史最低价
3. **可用性变化**：某些区域可能暂时售罄
4. **计费单位**：价格为每小时费用

## 🔗 相关链接

- AWS 官方定价：https://aws.amazon.com/ec2/pricing/
- instances.vantage.sh：https://instances.vantage.sh/
- ec2instances.info 源码：https://github.com/vantage-sh/ec2instances.info

## 💬 反馈

如有问题或建议，欢迎提出！

---

**创建时间**：2025-11-30
**版本**：1.0
**状态**：✅ 生产就绪
